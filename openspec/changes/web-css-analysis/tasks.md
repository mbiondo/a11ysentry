# Tasks: Web CSS Analysis

- [ ] 1.1 Implement CSS extraction logic in `adapters/web/adapter.go`.
- [ ] 1.2 Update `traverse` to look up and merge CSS class traits.
- [ ] 1.3 Implement conflict resolution (inline styles override classes).
- [ ] 1.4 Add unit tests in `adapters/web/adapter_test.go` covering multiple classes and overrides.
- [ ] 1.5 Verify end-to-end: ensure the domain `Analyzer` correctly reports contrast violations for class-styled elements.
